
/**
 * nodelike is a type like the jQuery object.
 * It can be converted from and implies to be either a: {@type {Node}}, {@type {Array.<Node>}}, {@type {SelectorString}}
  * @file
 * @typedef {NodeLike}
 *
 */

 /**
  * a selector string is simply a {@type {String} string} that addresses a css selector
  * @file
  * @typedef {SelectorString}
  */

/**
 * a simple integer type
 * it is really nothing but a {@type {Number}}
 * @file
 * @typedef {Integer}
 */

/**
 * a vue VM like described {@link https://vuejs.org/v2/guide/instance.html here}
 *
 *[link to Google!](http://google.com)
 * @file
 * @typedef {VueComponentInstance}
 */